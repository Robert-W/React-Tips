.app-body
  flex-direction column
  display flex
  padding-bottom 20px
  position absolute
  top $headerHeight
  overflow-y auto
  width 100%
  bottom 0
  left 0

.inner-width
  margin $sectionMargin auto
  width $innerWidth
